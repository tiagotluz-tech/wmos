{
  "name": "@cgw/jobs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests configured for @cgw/jobs yet\""
  },
  "devDependencies": {
    "tsx": "latest"
  }
}
